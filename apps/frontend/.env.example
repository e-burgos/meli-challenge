# Frontend env — copy to .env.development and .env.production as needed.
# Only VITE_* vars are exposed to the client (Vite).

# API base URL for backend requests.
# - Development: /api (Vite proxy forwards to backend on localhost:3333).
# - Production: full URL of your deployed backend, e.g. https://your-app.onrender.com/api
VITE_API_BASE_URL=/api
