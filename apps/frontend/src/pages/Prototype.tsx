/**
 * Prototype — How to use each component (T015b).
 * Examples and variants for Button, Card, Layout, etc.
 * Route: /prototype
 */
export function Prototype() {
  return (
    <main className="p-6 max-w-4xl mx-auto">
      <h1 className="text-2xl font-semibold mb-4">Prototype — Uso de componentes</h1>
      <p className="text-gray-600">
        Prototipo que indica cómo utilizar cada componente (ejemplos, variantes).
        Contenido por completar según T015b.
      </p>
    </main>
  );
}
