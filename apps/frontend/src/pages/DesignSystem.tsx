/**
 * Design System — Visual documentation for developers (T015b).
 * Define design-spec-meli tokens, typography, colors, spacing, and component examples.
 * Route: /design-system
 */
export function DesignSystem() {
  return (
    <main className="p-6 max-w-4xl mx-auto">
      <h1 className="text-2xl font-semibold mb-4">Design System — Meli</h1>
      <p className="text-gray-600">
        Documentación visual del design system (tokens, tipografía, colores, espaciado, componentes).
        Contenido por completar según T015b.
      </p>
    </main>
  );
}
