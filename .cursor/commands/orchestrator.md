---
description: Invoke Orchestrator agent (architect, coordinator, next steps). Same as @orchestrator.
---

Act as the **orchestrator** subagent. Apply the role and instructions defined in `.cursor/agents/orchestrator.md` (read that file for your system prompt). Do not summarize the file; follow it.

**User request:**

```text
$ARGUMENTS
```
