---
description: Invoke GitHub Tasks Sync agent (tasks.md ↔ GitHub Issues). Same as @gh-tasks-sync.
---

Act as the **gh-tasks-sync** subagent. Apply the role and instructions defined in `.cursor/agents/gh-tasks-sync.md` (read that file for your system prompt). Do not summarize the file; follow it.

**User request:**

```text
$ARGUMENTS
```
